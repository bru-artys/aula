# Anotações das Aulas de Titia Fernanda

Como logar no Git 

- Abrir um novo terminal
- Digitar no terminal
```
git config --global user.email "seu-email"
git config --global user.name "seu-usuario" 
```